<div class="auth-form">
  <div class="form-box">
    <!-- <div class="button-box">
      <div class="button1" id="button"></div>
      <button class="toggle-btn" type="button" onclick="login()">Log In</button>
      <button class="toggle-btn" type="button" onclick="register()">
        Register
      </button>
    </div> -->
    <!-- <div class="social-icons-facebookicon">
      <i class="fa fa-facebook facebook"></i>
    </div>
    <div class="social-icons-googleicon">
      <i class="fa fa-google-plus google"></i>
    </div> -->
    <h2 style="color: white; margin-top: 10px">RESET PASSWORD</h2>
    <div class="input-group">
      <% if (msg) { %> <% if (msg === "Message sent to registered email") { %>
      <div class="alert alert-success" role="alert"><%= msg %></div>
      <% } else {%>
      <div class="alert alert-danger" role="alert"><%= msg %></div>
      <% } %> <% } %>
      <form action="/resetpassword" method="POST" id="Login">
        <input
          type="text"
          class="input-field"
          placeholder="University ID"
          name="univId"
          required
        />
        <input
          type="email"
          class="input-field"
          name="email"
          id=""
          placeholder="email"
          required
        />
        <button class="btn btn-primary signin">Send Email</button>
      </form>
    </div>
  </div>
</div>
